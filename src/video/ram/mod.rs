// Video memory. Connected to the B bus.

mod cgram;

// Struct containing OAM, CGRAM and VRAM.
pub struct VideoMem {
    // OAM
    // CGRAM
    // VRAM
}

impl VideoMem {
    pub fn new() -> Self {
        VideoMem {
            
        }
    }
}